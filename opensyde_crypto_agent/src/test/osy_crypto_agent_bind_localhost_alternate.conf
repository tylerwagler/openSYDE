OSY_CRYPTO_AGENT_TCP_SERVER_PORT=50963

# set bind address to localhost; but not the default 127.0.0.1
# so the server will start with a valid bind address
# but the client will not be able to connect to it
OSY_CRYPTO_AGENT_TCP_SERVER_BIND_ADDRESS=127.0.0.2
OSY_CRYPTO_AGENT_LOG_FILE_FOLDER=./logs
OSY_CRYPTO_AGENT_AUTH_PEM_FILE_FOLDER=./../src/test/certificates
